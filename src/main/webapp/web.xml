<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="http://xmlns.jcp.org/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee 
         http://xmlns.jcp.org/xml/ns/javaee/web-app_4_0.xsd"
         version="4.0">

    <!-- Configuración básica -->
    <display-name>MiProyectoWeb</display-name>
    <description>Proyecto en Tomcat 9</description>

    <!-- Páginas de inicio -->
    <welcome-file-list>
        <welcome-file>index.html</welcome-file>
        <welcome-file>index.jsp</welcome-file>
    </welcome-file-list>

    <!-- Ejemplo de Servlet (sin anotaciones) -->
    <servlet>
        <servlet-name>MiServlet</servlet-name>
        <servlet-class>com.ejemplo.servlets.MiServlet</servlet-class>
        <init-param>
            <param-name>param1</param-name>
            <param-value>valor1</param-value>
        </init-param>
    </servlet>

    <servlet-mapping>
        <servlet-name>MiServlet</servlet-name>
        <url-pattern>/mi-servlet</url-pattern>
    </servlet-mapping>

    <!-- Configuración de Encoding UTF-8 -->
    <filter>
        <filter-name>EncodingFilter</filter-name>
        <filter-class>com.ejemplo.filters.EncodingFilter</filter-class>
        <init-param>
            <param-name>encoding</param-name>
            <param-value>UTF-8</param-value>
        </init-param>
    </filter>

    <filter-mapping>
        <filter-name>EncodingFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <!-- Tiempo de sesión -->
    <session-config>
        <session-timeout>30</session-timeout> <!-- En minutos -->
    </session-config>

</web-app>